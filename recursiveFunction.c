#include <stdio.h>
long int multiply numbers (int n);
int main(){
    int n;
    printf("Enter a positive integer:");
    scanf("Factorial of %d=1%d",n,multiply number(n));
    return 0;
}
long int multiply number(int n){
    if(n>=1)
    return n*multiply number (n-1);
    else
    return 1;
}